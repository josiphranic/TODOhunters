@model List<urednistvo.ModelsView.Textual.EditionView>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.TimeOfRelease)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.NumberOfTexts }) |
            @Html.ActionLink("Details", "Details", new { id=item.NumberOfTexts }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.NumberOfTexts })
        </td>
    </tr>
}

</table>
